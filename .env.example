# Optional production PostgreSQL connection string. When omitted, the site uses
# a local SQLite database automatically (database/martia.sqlite).
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/martia_barber
PORT=3000
