# PostgreSQL connection string for Vercel / Neon production bookings.
# In Vercel, connect the Neon integration; it adds DATABASE_URL automatically.
# DATABASE_URL=postgres://user:password@host/database?sslmode=require
PORT=3000
